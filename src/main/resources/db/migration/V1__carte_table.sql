CREATE TABLE carte(
                      id       INT PRIMARY KEY,
                      denumire VARCHAR(100) NOT NULL,
                      autor    VARCHAR(100) NOT NULL,
                      an       INT          NOT NULL
);