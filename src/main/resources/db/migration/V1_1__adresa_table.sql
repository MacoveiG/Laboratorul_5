CREATE TABLE adresa(
                       id            INT PRIMARY KEY,
                       tara          VARCHAR(100) NOT NULL,
                       oras          VARCHAR(100) NOT NULL,
                       strada        VARCHAR(100),
                       nr_casa       INT,
                       nr_apartament INT
);